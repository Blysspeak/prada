# Документация PRADA

## Обзор

PRADA (PRisma ADmin) — это универсальная панель администратора с открытым исходным кодом для любых проектов, использующих Prisma ORM. Она автоматически генерирует интерфейс CRUD из вашего файла `schema.prisma`.

## Архитектура

Проект состоит из трех основных пакетов:

### 1. @prada/core

Этот пакет отвечает за:
- Парсинг схемы Prisma
- Генерацию API
- Аутентификацию (JWT, bcrypt)
- Валидацию данных (Zod)

Основные зависимости:
- `@prisma/internals` — для работы с внутренними компонентами Prisma
- `jsonwebtoken` — для работы с JWT-токенами
- `zod` — для валидации данных

### 2. @prada/server

Этот пакет предоставляет:
- Express-сервер
- Middleware для интеграции с существующими приложениями
- Роутинг для API-эндпоинтов

Основные зависимости:
- `@prada/core` — для использования функций ядра
- `express` — веб-фреймворк
- `cors` — для управления CORS
- `cookie-parser` — для работы с куки

### 3. @prada/ui

Этот пакет содержит:
- Пользовательский интерфейс на React
- Компоненты для отображения данных
- Формы для создания и редактирования записей
- Настройки темы и языка

Основные зависимости:
- `react` и `react-dom` — основа пользовательского интерфейса
- `@tanstack/react-table` — для создания таблиц
- `@tanstack/react-query` — для управления состоянием
- `react-hook-form` — для работы с формами
- `react-router-dom` — для маршрутизации

## Конфигурация

### Настройка моделей

Вы можете настроить поведение конкретных моделей, указав параметры в объекте `models`:

```typescript
{
  models: {
    User: {
      fields: {
        password: { hidden: true }, // Скрыть поле
        email: { searchable: true } // Сделать searchable
      },
      actions: ['create', 'read', 'update'] // Ограничить действия
    }
  }
}
```

### Параметры запросов

API поддерживает следующие параметры запросов:
- `page` — номер страницы (по умолчанию: 1)
- `limit` — количество записей на странице (по умолчанию: 20, максимум: 100)
- `sort` — поле для сортировки
- `order` — порядок сортировки (asc/desc)
- `search` — поисковый запрос
- `include` — связи для включения (через запятую)

## Темизация

PRADA использует CSS-переменные для согласованного оформления. Все цвета определены в файле `packages/ui/src/index.css`:

```css
:root, [data-theme="dark"] {
  --bg-base: #0a0f1a;
  --bg-primary: #0f172a;
  --bg-secondary: #1e293b;
  --text-primary: #f1f5f9;
  --text-secondary: #e2e8f0;
  --primary: #3b82f6;
  /* ... другие переменные */
}

[data-theme="light"] {
  --bg-base: #f1f5f9;
  --bg-primary: #ffffff;
  /* ... переопределения для светлой темы */
}
```

Для настройки цветов просто переопределите CSS-переменные.

## Интернационализация (i18n)

PRADA поддерживает несколько языков. В настоящее время доступны:
- 🇷🇺 Русский (ru)
- 🇬🇧 Английский (en)

Язык можно изменить в настройках. Переводы хранятся в файле `packages/ui/src/i18n/translations.ts`.

Для добавления нового языка:
1. Добавьте переводы в `translations.ts`
2. Обновите селектор языка в настройках

## Поддерживаемые типы полей

| Тип Prisma | Компонент UI |
|-------------|--------------|
| String | Текстовое поле ввода |
| Int, Float, Decimal, BigInt | Числовое поле ввода |
| Boolean | Чекбокс |
| DateTime | Выбор даты и времени |
| Enum | Выпадающий список |
| Json | Текстовое поле JSON |
| Relations | Просмотр/ссылка (только для чтения) |

## API-эндпоинты

| Метод | Эндпоинт | Описание |
|--------|----------|-------------|
| GET | `/admin/api/schema` | Получить метаданные всех моделей |
| GET | `/admin/api/:model` | Список записей (с пагинацией) |
| GET | `/admin/api/:model/:id` | Получить одну запись |
| POST | `/admin/api/:model` | Создать запись |
| PUT | `/admin/api/:model/:id` | Обновить запись |
| DELETE | `/admin/api/:model/:id` | Удалить запись |
| POST | `/admin/api/auth/login` | Вход |
| POST | `/admin/api/auth/logout` | Выход |
| GET | `/admin/api/auth/me` | Текущий пользователь |

## Разработка

### Запуск в режиме разработки

Для запуска в режиме разработки выполните:

```bash
# Запуск в параллельном режиме
pnpm dev

# Или запуск конкретного пакета
cd packages/ui
pnpm dev
```

### Сборка

Для сборки всех пакетов выполните:

```bash
pnpm build
```

### Линтинг

Для проверки кода с помощью ESLint выполните:

```bash
pnpm lint
```

## Лицензия

MIT